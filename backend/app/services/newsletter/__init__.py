"""Newsletter Composer — orchestration, voice, sections, exports.

v1 ships the composer shell (CRUD + draft→sent workflow). AI generation,
voice tuning, and export formats arrive in later phases.
"""
